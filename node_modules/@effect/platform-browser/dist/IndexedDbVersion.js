import { pipeArguments } from "effect/Pipeable";
const TypeId = "~@effect/platform-browser/IndexedDbVersion";
const Proto = {
  [TypeId]: TypeId,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
const makeProto = options => {
  // oxlint-disable-next-line typescript/no-extraneous-class
  class Version {}
  Object.assign(Version, Proto);
  Version.tables = options.tables;
  return Version;
};
/**
 * @since 4.0.0
 * @category constructors
 */
export const make = (...tables) => makeProto({
  tables: new Map(tables.map(table => [table.tableName, table]))
});
//# sourceMappingURL=IndexedDbVersion.js.map