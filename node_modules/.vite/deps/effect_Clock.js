import { n as __exportAll } from "./chunk-EAsCxrDo.js";
import { mt as currentTimeNanos$1, pt as currentTimeMillis$1, r as ClockRef, st as clockWith$1 } from "./effect-WSSJVTeC.js";
//#region node_modules/effect/dist/Clock.js
var Clock_exports = /* @__PURE__ */ __exportAll({
	Clock: () => Clock,
	clockWith: () => clockWith,
	currentTimeMillis: () => currentTimeMillis,
	currentTimeNanos: () => currentTimeNanos
});
/**
* A reference to the current Clock service in the environment.
*
* @example
* ```ts
* import { Clock, Effect } from "effect"
*
* const program = Effect.gen(function*() {
*   const clock = yield* Clock.Clock
*   return clock.currentTimeMillisUnsafe()
* })
* ```
*
* @category references
* @since 4.0.0
*/
var Clock = ClockRef;
/**
* Accesses the current Clock service and uses it to run the provided function.
*
* @example
* ```ts
* import { Clock, Effect } from "effect"
*
* const program = Clock.clockWith((clock) =>
*   Effect.sync(() => {
*     const currentTime = clock.currentTimeMillisUnsafe()
*     console.log(`Current time: ${currentTime}`)
*     return currentTime
*   })
* )
* ```
*
* @category constructors
* @since 2.0.0
*/
var clockWith = clockWith$1;
/**
* Returns an Effect that succeeds with the current time in milliseconds.
*
* @example
* ```ts
* import { Clock, Effect } from "effect"
*
* const program = Effect.gen(function*() {
*   const currentTime = yield* Clock.currentTimeMillis
*   console.log(`Current time: ${currentTime}ms`)
*   return currentTime
* })
* ```
*
* @category constructors
* @since 2.0.0
*/
var currentTimeMillis = currentTimeMillis$1;
/**
* Returns an Effect that succeeds with the current time in nanoseconds.
*
* @example
* ```ts
* import { Clock, Effect } from "effect"
*
* const program = Effect.gen(function*() {
*   const currentTime = yield* Clock.currentTimeNanos
*   console.log(`Current time: ${currentTime}ns`)
*   return currentTime
* })
* ```
*
* @category constructors
* @since 2.0.0
*/
var currentTimeNanos = currentTimeNanos$1;
//#endregion
export { Clock, clockWith, currentTimeMillis, currentTimeNanos, Clock_exports as t };

//# sourceMappingURL=effect_Clock.js.map