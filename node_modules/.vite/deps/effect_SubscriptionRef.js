import { n as __exportAll } from "./chunk-EAsCxrDo.js";
import { _ as identity, p as dual } from "./Function-CAyx_go8.js";
import { hasProperty } from "./effect_Predicate.js";
import { m as PipeInspectableProto } from "./core-CH33zrUT.js";
import { isNone } from "./effect_Option.js";
import { Yt as map, dr as succeed, hr as sync, mr as suspend } from "./Effect-X-nh5NFG.js";
import { A as unbounded, v as publishUnsafe } from "./PubSub-CCYPM6Ux.js";
import { Hi as makeUnsafe, Tt as fromPubSub } from "./Stream-BYsS03xU.js";
//#region node_modules/effect/dist/SubscriptionRef.js
/**
* @since 2.0.0
*/
var SubscriptionRef_exports = /* @__PURE__ */ __exportAll({
	changes: () => changes,
	get: () => get,
	getAndSet: () => getAndSet,
	getAndUpdate: () => getAndUpdate,
	getAndUpdateEffect: () => getAndUpdateEffect,
	getAndUpdateSome: () => getAndUpdateSome,
	getAndUpdateSomeEffect: () => getAndUpdateSomeEffect,
	getUnsafe: () => getUnsafe,
	isSubscriptionRef: () => isSubscriptionRef,
	make: () => make,
	modify: () => modify,
	modifyEffect: () => modifyEffect,
	modifySome: () => modifySome,
	modifySomeEffect: () => modifySomeEffect,
	set: () => set,
	setAndGet: () => setAndGet,
	update: () => update,
	updateAndGet: () => updateAndGet,
	updateAndGetEffect: () => updateAndGetEffect,
	updateEffect: () => updateEffect,
	updateSome: () => updateSome,
	updateSomeAndGet: () => updateSomeAndGet,
	updateSomeAndGetEffect: () => updateSomeAndGetEffect,
	updateSomeEffect: () => updateSomeEffect
});
var TypeId = "~effect/SubscriptionRef";
/**
* @since 4.0.0
* @category guards
*/
var isSubscriptionRef = (u) => hasProperty(u, TypeId);
var Proto = {
	...PipeInspectableProto,
	[TypeId]: { _A: identity },
	toJSON() {
		return {
			_id: "SubscriptionRef",
			value: this.value
		};
	}
};
/**
* Constructs a new `SubscriptionRef` from an initial value.
*
* @since 2.0.0
* @category constructors
*/
var make = (value) => map(unbounded({ replay: 1 }), (pubsub) => {
	const self = Object.create(Proto);
	self.semaphore = makeUnsafe(1);
	self.value = value;
	self.pubsub = pubsub;
	publishUnsafe(self.pubsub, value);
	return self;
});
/**
* Creates a stream that emits the current value and all subsequent changes to
* the `SubscriptionRef`.
*
* The stream will first emit the current value, then emit all future changes
* as they occur.
*
* @example
* ```ts
* import { Effect, Stream, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(0)
*
*   const stream = SubscriptionRef.changes(ref)
*
*   const fiber = yield* Stream.runForEach(
*     stream,
*     (value) => Effect.sync(() => console.log("Value:", value))
*   ).pipe(Effect.forkScoped)
*
*   yield* SubscriptionRef.set(ref, 1)
*   yield* SubscriptionRef.set(ref, 2)
* })
* ```
*
* @category changes
* @since 2.0.0
*/
var changes = (self) => fromPubSub(self.pubsub);
/**
* Unsafely retrieves the current value of the `SubscriptionRef`.
*
* This function directly accesses the underlying reference without any
* synchronization. It should only be used when you're certain there are no
* concurrent modifications.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(42)
*
*   const value = SubscriptionRef.getUnsafe(ref)
*   console.log(value)
* })
* ```
*
* @since 2.0.0
* @category getters
*/
var getUnsafe = (self) => self.value;
/**
* Retrieves the current value of the `SubscriptionRef`.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(42)
*
*   const value = yield* SubscriptionRef.get(ref)
*   console.log(value)
* })
* ```
*
* @since 2.0.0
* @category getters
*/
var get = (self) => sync(() => self.value);
/**
* Atomically retrieves the current value and sets a new value, notifying
* subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const oldValue = yield* SubscriptionRef.getAndSet(ref, 20)
*   console.log("Old value:", oldValue)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category getters
*/
var getAndSet = /*#__PURE__*/ dual(2, (self, value) => self.semaphore.withPermit(sync(() => {
	const current = self.value;
	setUnsafe(self, value);
	return current;
})));
var setUnsafe = (self, value) => {
	self.value = value;
	publishUnsafe(self.pubsub, value);
};
/**
* Atomically retrieves the current value and updates it with the result of
* applying a function, notifying subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const oldValue = yield* SubscriptionRef.getAndUpdate(ref, (n) => n * 2)
*   console.log("Old value:", oldValue)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category getters
*/
var getAndUpdate = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(sync(() => {
	const current = self.value;
	setUnsafe(self, update(current));
	return current;
})));
/**
* Atomically retrieves the current value and updates it with the result of
* applying an effectful function, notifying subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const oldValue = yield* SubscriptionRef.getAndUpdateEffect(
*     ref,
*     (n) => Effect.succeed(n + 5)
*   )
*   console.log("Old value:", oldValue)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category getters
*/
var getAndUpdateEffect = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(sync(() => {
	const current = self.value;
	return map(update(current), (newValue) => {
		setUnsafe(self, newValue);
		return current;
	});
})));
/**
* Atomically retrieves the current value and optionally updates it with the
* result of applying a function that returns an `Option`, notifying
* subscribers only if the value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const oldValue = yield* SubscriptionRef.getAndUpdateSome(
*     ref,
*     (n) => n > 5 ? Option.some(n * 2) : Option.none()
*   )
*   console.log("Old value:", oldValue)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category getters
*/
var getAndUpdateSome = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(sync(() => {
	const current = self.value;
	const option = update(current);
	if (isNone(option)) return succeed(current);
	setUnsafe(self, option.value);
	return current;
})));
/**
* Atomically retrieves the current value and optionally updates it with the
* result of applying an effectful function that returns an `Option`,
* notifying subscribers only if the value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const oldValue = yield* SubscriptionRef.getAndUpdateSomeEffect(
*     ref,
*     (n) => Effect.succeed(n > 5 ? Option.some(n + 3) : Option.none())
*   )
*   console.log("Old value:", oldValue)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category getters
*/
var getAndUpdateSomeEffect = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(suspend(() => {
	const current = self.value;
	return map(update(current), (option) => {
		if (isNone(option)) return current;
		setUnsafe(self, option.value);
		return current;
	});
})));
/**
* Atomically modifies the `SubscriptionRef` with a function that computes a
* return value and a new value, notifying subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const result = yield* SubscriptionRef.modify(ref, (n) => [
*     `Old value was ${n}`,
*     n * 2
*   ])
*   console.log(result)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category modifications
*/
var modify = /*#__PURE__*/ dual(2, (self, modify) => self.semaphore.withPermit(sync(() => {
	const [b, newValue] = modify(self.value);
	setUnsafe(self, newValue);
	return b;
})));
/**
* Atomically modifies the `SubscriptionRef` with an effectful function that
* computes a return value and a new value, notifying subscribers of the
* change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const result = yield* SubscriptionRef.modifyEffect(
*     ref,
*     (n) => Effect.succeed([`Doubled from ${n}`, n * 2] as const)
*   )
*   console.log(result)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category modifications
*/
var modifyEffect = /*#__PURE__*/ dual(2, (self, modify) => self.semaphore.withPermit(suspend(() => map(modify(self.value), ([b, newValue]) => {
	setUnsafe(self, newValue);
	return b;
}))));
/**
* Atomically modifies the `SubscriptionRef` with a function that computes a
* return value and optionally a new value, notifying subscribers only if the
* value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const result = yield* SubscriptionRef.modifySome(
*     ref,
*     (n) =>
*       n > 5 ? ["Updated", Option.some(n * 2)] : ["Not updated", Option.none()]
*   )
*   console.log(result)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category modifications
*/
var modifySome = /*#__PURE__*/ dual(2, (self, modify) => self.semaphore.withPermit(sync(() => {
	const [b, option] = modify(self.value);
	if (isNone(option)) return b;
	setUnsafe(self, option.value);
	return b;
})));
/**
* Atomically modifies the `SubscriptionRef` with an effectful function that
* computes a return value and optionally a new value, notifying subscribers
* only if the value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const result = yield* SubscriptionRef.modifySomeEffect(
*     ref,
*     (n) =>
*       Effect.succeed(
*         n > 5
*           ? (["Updated", Option.some(n + 5)] as const)
*           : (["Not updated", Option.none()] as const)
*       )
*   )
*   console.log(result)
*
*   const newValue = yield* SubscriptionRef.get(ref)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category modifications
*/
var modifySomeEffect = /*#__PURE__*/ dual(2, (self, modify) => self.semaphore.withPermit(suspend(() => map(modify(self.value), ([b, option]) => {
	if (isNone(option)) return b;
	setUnsafe(self, option.value);
	return b;
}))));
/**
* Sets the value of the `SubscriptionRef`, notifying all subscribers of the
* change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(0)
*
*   yield* SubscriptionRef.set(ref, 42)
*
*   const value = yield* SubscriptionRef.get(ref)
*   console.log(value)
* })
* ```
*
* @since 2.0.0
* @category setters
*/
var set = /*#__PURE__*/ dual(2, (self, value) => self.semaphore.withPermit(sync(() => setUnsafe(self, value))));
/**
* Sets the value of the `SubscriptionRef` and returns the new value,
* notifying all subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(0)
*
*   const newValue = yield* SubscriptionRef.setAndGet(ref, 42)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category setters
*/
var setAndGet = /*#__PURE__*/ dual(2, (self, value) => self.semaphore.withPermit(sync(() => {
	setUnsafe(self, value);
	return value;
})));
/**
* Updates the value of the `SubscriptionRef` with the result of applying a
* function, notifying subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   yield* SubscriptionRef.update(ref, (n) => n * 2)
*
*   const value = yield* SubscriptionRef.get(ref)
*   console.log(value)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var update = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(sync(() => setUnsafe(self, update(self.value)))));
/**
* Updates the value of the `SubscriptionRef` with the result of applying an
* effectful function, notifying subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   yield* SubscriptionRef.updateEffect(ref, (n) => Effect.succeed(n + 5))
*
*   const value = yield* SubscriptionRef.get(ref)
*   console.log(value)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var updateEffect = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(suspend(() => map(update(self.value), (newValue) => setUnsafe(self, newValue)))));
/**
* Updates the value of the `SubscriptionRef` with the result of applying a
* function and returns the new value, notifying subscribers of the change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const newValue = yield* SubscriptionRef.updateAndGet(ref, (n) => n * 2)
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var updateAndGet = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(sync(() => {
	const newValue = update(self.value);
	setUnsafe(self, newValue);
	return newValue;
})));
/**
* Updates the value of the `SubscriptionRef` with the result of applying an
* effectful function and returns the new value, notifying subscribers of the
* change.
*
* @example
* ```ts
* import { Effect, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const newValue = yield* SubscriptionRef.updateAndGetEffect(
*     ref,
*     (n) => Effect.succeed(n + 5)
*   )
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var updateAndGetEffect = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(suspend(() => map(update(self.value), (newValue) => {
	setUnsafe(self, newValue);
	return newValue;
}))));
/**
* Optionally updates the value of the `SubscriptionRef` with the result of
* applying a function that returns an `Option`, notifying subscribers only if
* the value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   yield* SubscriptionRef.updateSome(
*     ref,
*     (n) => n > 5 ? Option.some(n * 2) : Option.none()
*   )
*
*   const value = yield* SubscriptionRef.get(ref)
*   console.log(value)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var updateSome = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(sync(() => {
	const option = update(self.value);
	if (isNone(option)) return;
	setUnsafe(self, option.value);
})));
/**
* Optionally updates the value of the `SubscriptionRef` with the result of
* applying an effectful function that returns an `Option`, notifying
* subscribers only if the value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   yield* SubscriptionRef.updateSomeEffect(
*     ref,
*     (n) => Effect.succeed(n > 5 ? Option.some(n + 3) : Option.none())
*   )
*
*   const value = yield* SubscriptionRef.get(ref)
*   console.log(value)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var updateSomeEffect = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(suspend(() => map(update(self.value), (option) => {
	if (isNone(option)) return;
	setUnsafe(self, option.value);
}))));
/**
* Optionally updates the value of the `SubscriptionRef` with the result of
* applying a function that returns an `Option` and returns the new value,
* notifying subscribers only if the value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const newValue = yield* SubscriptionRef.updateSomeAndGet(
*     ref,
*     (n) => n > 5 ? Option.some(n * 2) : Option.none()
*   )
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var updateSomeAndGet = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(sync(() => {
	const current = self.value;
	const option = update(current);
	if (isNone(option)) return current;
	setUnsafe(self, option.value);
	return option.value;
})));
/**
* Optionally updates the value of the `SubscriptionRef` with the result of
* applying an effectful function that returns an `Option` and returns the new
* value, notifying subscribers only if the value changes.
*
* @example
* ```ts
* import { Effect, Option, SubscriptionRef } from "effect"
*
* const program = Effect.gen(function*() {
*   const ref = yield* SubscriptionRef.make(10)
*
*   const newValue = yield* SubscriptionRef.updateSomeAndGetEffect(
*     ref,
*     (n) => Effect.succeed(n > 5 ? Option.some(n + 3) : Option.none())
*   )
*   console.log("New value:", newValue)
* })
* ```
*
* @since 2.0.0
* @category updating
*/
var updateSomeAndGetEffect = /*#__PURE__*/ dual(2, (self, update) => self.semaphore.withPermit(suspend(() => {
	const current = self.value;
	return map(update(current), (option) => {
		if (isNone(option)) return current;
		setUnsafe(self, option.value);
		return option.value;
	});
})));
//#endregion
export { changes, get, getAndSet, getAndUpdate, getAndUpdateEffect, getAndUpdateSome, getAndUpdateSomeEffect, getUnsafe, isSubscriptionRef, make, modify, modifyEffect, modifySome, modifySomeEffect, set, setAndGet, SubscriptionRef_exports as t, update, updateAndGet, updateAndGetEffect, updateEffect, updateSome, updateSomeAndGet, updateSomeAndGetEffect, updateSomeEffect };

//# sourceMappingURL=effect_SubscriptionRef.js.map