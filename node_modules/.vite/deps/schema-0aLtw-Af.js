import { kt as TaggedStruct } from "./Schema-D3WN1j11.js";
//#region node_modules/foldkit/dist/schema/index.js
var makeCallable = (schema) => new Proxy(function() {}, {
	apply(_target, _thisArg, argumentsList) {
		return schema.make(argumentsList[0] ?? {});
	},
	get(_target, property, receiver) {
		return Reflect.get(schema, property, receiver);
	},
	has(_target, property) {
		return Reflect.has(schema, property);
	},
	getPrototypeOf() {
		return Reflect.getPrototypeOf(schema);
	}
});
function m(tag, fields = {}) {
	return makeCallable(TaggedStruct(tag, fields));
}
function r(tag, fields = {}) {
	return makeCallable(TaggedStruct(tag, fields));
}
function ts(tag, fields = {}) {
	return makeCallable(TaggedStruct(tag, fields));
}
//#endregion
export { r as n, ts as r, m as t };

//# sourceMappingURL=schema-0aLtw-Af.js.map