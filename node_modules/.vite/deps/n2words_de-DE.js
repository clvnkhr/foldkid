import { n as parseCurrencyValue, r as parseCardinalValue, t as parseOrdinalValue } from "./parse-ordinal-CLTHoyNi.js";
import { t as validateOptions } from "./validate-options-BeWy1avT.js";
//#region node_modules/n2words/src/de-DE.js
/**
* German (Germany) language converter
*
* CLDR: de-DE | German as used in Germany
*
* Key features:
* - Inverted tens-ones order: "einundzwanzig" (one-and-twenty) for 21-99
* - Compound words without spaces below million level
* - Three forms of 1: "eins" (standalone), "ein" (before hundert/tausend), "eine" (before Million+)
* - Scale pluralization: Million → Millionen, Milliarde → Milliarden
* - Spaces only around million+ scale words
* - BigInt modulo for efficient segment extraction
*/
var ONES = [
	"",
	"eins",
	"zwei",
	"drei",
	"vier",
	"fünf",
	"sechs",
	"sieben",
	"acht",
	"neun"
];
var EIN = "ein";
var TEENS = [
	"zehn",
	"elf",
	"zwölf",
	"dreizehn",
	"vierzehn",
	"fünfzehn",
	"sechzehn",
	"siebzehn",
	"achtzehn",
	"neunzehn"
];
var TENS = [
	"",
	"",
	"zwanzig",
	"dreißig",
	"vierzig",
	"fünfzig",
	"sechzig",
	"siebzig",
	"achtzig",
	"neunzig"
];
var SCALES = [
	"tausend",
	"Million",
	"Milliarde",
	"Billion",
	"Billiarde",
	"Trillion",
	"Trilliarde",
	"Quadrillion",
	"Quadrilliarde"
];
var SCALES_PLURAL = [
	"tausend",
	"Millionen",
	"Milliarden",
	"Billionen",
	"Billiarden",
	"Trillionen",
	"Trilliarden",
	"Quadrillionen",
	"Quadrilliarden"
];
var HUNDRED = "hundert";
var ZERO = "null";
var ORDINAL_ONES = [
	"",
	"erste",
	"zweite",
	"dritte",
	"vierte",
	"fünfte",
	"sechste",
	"siebte",
	"achte",
	"neunte"
];
var ORDINAL_TEENS = [
	"zehnte",
	"elfte",
	"zwölfte",
	"dreizehnte",
	"vierzehnte",
	"fünfzehnte",
	"sechzehnte",
	"siebzehnte",
	"achtzehnte",
	"neunzehnte"
];
var ORDINAL_TENS = [
	"",
	"",
	"zwanzigste",
	"dreißigste",
	"vierzigste",
	"fünfzigste",
	"sechzigste",
	"siebzigste",
	"achtzigste",
	"neunzigste"
];
var ORDINAL_SUFFIX = "ste";
/**
* Builds segment word for 0-999 (standalone form, uses "eins").
* German inverts ones and tens: "einundzwanzig" = one-and-twenty
*
* @param {number} n - Number 0-999
* @returns {string} German words for the segment
*/
function buildSegment(n) {
	if (n === 0) return "";
	const ones = n % 10;
	const tens = Math.trunc(n / 10) % 10;
	const hundreds = Math.trunc(n / 100);
	let result = "";
	if (hundreds > 0) result += (hundreds === 1 ? EIN : ONES[hundreds]) + HUNDRED;
	if (tens === 1) result += TEENS[ones];
	else if (tens >= 2 && ones > 0) result += (ones === 1 ? EIN : ONES[ones]) + "und" + TENS[tens];
	else if (tens >= 2) result += TENS[tens];
	else if (ones > 0) result += ONES[ones];
	return result;
}
/**
* Builds segment word for use before "tausend".
* Uses "ein" instead of "eins" for 1.
*
* @param {number} n - Number 0-999
* @returns {string} German words for thousand context
*/
function buildSegmentForThousand(n) {
	if (n === 0) return "";
	if (n === 1) return EIN;
	const ones = n % 10;
	const tens = Math.trunc(n / 10) % 10;
	const hundreds = Math.trunc(n / 100);
	let result = "";
	if (hundreds > 0) result += (hundreds === 1 ? EIN : ONES[hundreds]) + HUNDRED;
	if (tens === 1) result += TEENS[ones];
	else if (tens >= 2 && ones > 0) result += (ones === 1 ? EIN : ONES[ones]) + "und" + TENS[tens];
	else if (tens >= 2) result += TENS[tens];
	else if (ones > 0 && hundreds > 0) result += ONES[ones];
	else if (ones > 0) result += ONES[ones];
	return result;
}
/**
* Converts a non-negative integer to German words.
*
* @param {bigint} n - Non-negative integer to convert
* @returns {string} German words
*/
function integerToWords(n) {
	if (n === 0n) return ZERO;
	if (n < 1000n) return buildSegment(Number(n));
	if (n < 1000000n) {
		const thousands = Number(n / 1000n);
		const remainder = Number(n % 1000n);
		let result = buildSegmentForThousand(thousands) + SCALES[0];
		if (remainder > 0) result += buildSegment(remainder);
		return result;
	}
	return buildLargeNumberWords(n);
}
/**
* Builds words for numbers >= 1,000,000.
*
* @param {bigint} n - Number >= 1,000,000
* @returns {string} German words
*/
function buildLargeNumberWords(n) {
	const numStr = n.toString();
	const len = numStr.length;
	const segments = [];
	const segmentSize = 3;
	const remainderLen = len % segmentSize;
	let pos = 0;
	if (remainderLen > 0) {
		segments.push(Number(numStr.slice(0, remainderLen)));
		pos = remainderLen;
	}
	while (pos < len) {
		segments.push(Number(numStr.slice(pos, pos + segmentSize)));
		pos += segmentSize;
	}
	const parts = [];
	let scaleIndex = segments.length - 1;
	for (let i = 0; i < segments.length; i++) {
		const segment = segments[i];
		if (segment !== 0) if (scaleIndex === 0) parts.push({
			words: buildSegment(segment),
			isScale: false,
			scaleLevel: 0
		});
		else if (scaleIndex === 1) {
			const segWords = buildSegmentForThousand(segment);
			parts.push({
				words: segWords + SCALES[0],
				isScale: false,
				scaleLevel: 1
			});
		} else {
			let segWords;
			if (segment === 1) segWords = "eine";
			else segWords = buildSegment(segment);
			const scaleWord = segment === 1 ? SCALES[scaleIndex - 1] : SCALES_PLURAL[scaleIndex - 1];
			parts.push({
				words: segWords,
				isScale: false,
				scaleLevel: scaleIndex
			});
			parts.push({
				words: scaleWord,
				isScale: true,
				scaleLevel: scaleIndex
			});
		}
		scaleIndex--;
	}
	return joinGermanParts(parts);
}
/**
* Joins parts with German spacing rules.
* Spaces only around million+ scale words.
*
* @param {Array} parts - Parts with metadata
* @returns {string} Joined string
*/
function joinGermanParts(parts) {
	if (parts.length === 0) return ZERO;
	let result = "";
	for (let i = 0; i < parts.length; i++) {
		const part = parts[i];
		const prevPart = i > 0 ? parts[i - 1] : null;
		if (i > 0) {
			if (part.isScale || prevPart && prevPart.isScale) result += " ";
		}
		result += part.words;
	}
	return result;
}
/**
* Converts decimal digits to German words.
*
* @param {string} decimalPart - Decimal digits (without the point)
* @returns {string} German words for decimal part
*/
function decimalPartToWords(decimalPart) {
	let result = "";
	let i = 0;
	while (i < decimalPart.length && decimalPart[i] === "0") {
		if (result) result += " ";
		result += ZERO;
		i++;
	}
	const remainder = decimalPart.slice(i);
	if (remainder) {
		if (result) result += " ";
		result += integerToWords(BigInt(remainder));
	}
	return result;
}
/**
* Converts a numeric value to German words.
*
* This is the main public API. It accepts any valid numeric input
* (number, string, or bigint) and handles parsing internally.
*
* @param {number | string | bigint} value - The numeric value to convert
* @returns {string} The number in German words
* @throws {TypeError} If value is not a valid numeric type
* @throws {Error} If value is not a valid number format
*
* @example
* toCardinal(21)           // 'einundzwanzig'
* toCardinal(1000)         // 'eintausend'
* toCardinal(1000000)      // 'eine Million'
*/
function toCardinal(value) {
	const { isNegative, integerPart, decimalPart } = parseCardinalValue(value);
	let result = "";
	if (isNegative) result = "minus ";
	result += integerToWords(integerPart);
	if (decimalPart) result += " komma " + decimalPartToWords(decimalPart);
	return result;
}
/**
* Builds ordinal segment for 0-999.
* Only the final component becomes ordinal.
* Uses -te for 1-19, -ste for 20+.
*
* @param {number} n - Number 0-999
* @param {boolean} isFinal - Whether this is the final segment (gets ordinal suffix)
* @returns {string} German ordinal words for this segment
*/
function buildOrdinalSegment(n, isFinal) {
	if (n === 0) return "";
	const ones = n % 10;
	const tens = Math.trunc(n / 10) % 10;
	const hundreds = Math.trunc(n / 100);
	let result = "";
	if (hundreds > 0) result += (hundreds === 1 ? EIN : ONES[hundreds]) + HUNDRED;
	if (tens === 1) if (isFinal) result += ORDINAL_TEENS[ones];
	else result += TEENS[ones];
	else if (tens >= 2 && ones > 0) if (isFinal) result += (ones === 1 ? EIN : ONES[ones]) + "und" + ORDINAL_TENS[tens];
	else result += (ones === 1 ? EIN : ONES[ones]) + "und" + TENS[tens];
	else if (tens >= 2) if (isFinal) result += ORDINAL_TENS[tens];
	else result += TENS[tens];
	else if (ones > 0) if (isFinal) result += ORDINAL_ONES[ones];
	else result += ONES[ones];
	if (isFinal && hundreds > 0 && tens === 0 && ones === 0) result += ORDINAL_SUFFIX;
	return result;
}
/**
* Converts integer to German ordinal words.
*
* @param {bigint} n - Positive integer
* @returns {string} German ordinal words
*/
function integerToOrdinal(n) {
	if (n < 1000n) return buildOrdinalSegment(Number(n), true);
	if (n < 1000000n) {
		const thousands = Number(n / 1000n);
		const remainder = Number(n % 1000n);
		if (remainder === 0) return buildSegmentForThousand(thousands) + SCALES[0] + ORDINAL_SUFFIX;
		return buildSegmentForThousand(thousands) + SCALES[0] + buildOrdinalSegment(remainder, true);
	}
	return buildLargeOrdinal(n);
}
/**
* Builds ordinal words for numbers >= 1,000,000.
*
* @param {bigint} n - Number >= 1,000,000
* @returns {string} German ordinal words
*/
function buildLargeOrdinal(n) {
	const numStr = n.toString();
	const len = numStr.length;
	const segments = [];
	const segmentSize = 3;
	const remainderLen = len % segmentSize;
	let pos = 0;
	if (remainderLen > 0) {
		segments.push(Number(numStr.slice(0, remainderLen)));
		pos = remainderLen;
	}
	while (pos < len) {
		segments.push(Number(numStr.slice(pos, pos + segmentSize)));
		pos += segmentSize;
	}
	let lowestNonZeroIdx = 0;
	for (let i = segments.length - 1; i >= 0; i--) if (segments[i] !== 0) {
		lowestNonZeroIdx = i;
		break;
	}
	const parts = [];
	let scaleIndex = segments.length - 1;
	for (let i = 0; i < segments.length; i++) {
		const segment = segments[i];
		const isLowestSegment = i === lowestNonZeroIdx;
		if (segment !== 0) if (scaleIndex === 0) parts.push({
			words: buildOrdinalSegment(segment, true),
			isScale: false,
			scaleLevel: 0
		});
		else if (scaleIndex === 1) {
			const segWords = buildSegmentForThousand(segment);
			if (isLowestSegment) parts.push({
				words: segWords + SCALES[0] + ORDINAL_SUFFIX,
				isScale: false,
				scaleLevel: 1
			});
			else parts.push({
				words: segWords + SCALES[0],
				isScale: false,
				scaleLevel: 1
			});
		} else {
			let segWords;
			if (segment === 1) segWords = "eine";
			else segWords = buildSegment(segment);
			const scaleWord = segment === 1 ? SCALES[scaleIndex - 1] : SCALES_PLURAL[scaleIndex - 1];
			parts.push({
				words: segWords,
				isScale: false,
				scaleLevel: scaleIndex
			});
			if (isLowestSegment) parts.push({
				words: scaleWord + ORDINAL_SUFFIX,
				isScale: true,
				scaleLevel: scaleIndex
			});
			else parts.push({
				words: scaleWord,
				isScale: true,
				scaleLevel: scaleIndex
			});
		}
		scaleIndex--;
	}
	return joinGermanParts(parts);
}
/**
* Converts a numeric value to German ordinal words.
*
* German ordinals add -te for 1-19 and -ste for 20+.
* Irregular forms: erste (1st), dritte (3rd), siebte (7th), achte (8th).
*
* @param {number | string | bigint} value - The numeric value to convert (positive integer)
* @returns {string} The number as ordinal words
* @throws {TypeError} If value is not a valid numeric type
* @throws {RangeError} If value is negative, zero, or has a decimal part
*
* @example
* toOrdinal(1)    // 'erste'
* toOrdinal(2)    // 'zweite'
* toOrdinal(3)    // 'dritte'
* toOrdinal(21)   // 'einundzwanzigste'
* toOrdinal(100)  // 'einhundertste'
* toOrdinal(1000) // 'eintausendste'
*/
function toOrdinal(value) {
	return integerToOrdinal(parseOrdinalValue(value));
}
/**
* Converts a numeric value to German currency words (Euro).
*
* @param {number | string | bigint} value - The currency amount to convert
* @param {Object} [options] - Optional configuration
* @param {boolean} [options.and=true] - Use "und" between euros and cents
* @returns {string} The amount in German currency words
* @throws {TypeError} If value is not a valid numeric type
* @throws {Error} If value is not a valid number format
*
* @example
* toCurrency(42.50)                 // 'zweiundvierzig Euro und fünfzig Cent'
* toCurrency(1)                     // 'ein Euro'
* toCurrency(0.99)                  // 'neunundneunzig Cent'
* toCurrency(0.01)                  // 'ein Cent'
* toCurrency(42.50, { and: false }) // 'zweiundvierzig Euro fünfzig Cent'
*/
function toCurrency(value, options) {
	options = validateOptions(options);
	const { isNegative, dollars: euros, cents } = parseCurrencyValue(value);
	const { and: useAnd = true } = options;
	let result = "";
	if (isNegative) result = "minus ";
	if (euros > 0n || cents === 0n) {
		if (euros === 1n) result += EIN;
		else result += integerToWords(euros);
		result += " Euro";
	}
	if (cents > 0n) {
		if (euros > 0n) result += useAnd ? " und " : " ";
		if (cents === 1n) result += EIN;
		else result += integerToWords(cents);
		result += " Cent";
	}
	return result;
}
//#endregion
export { toCardinal, toCurrency, toOrdinal };

//# sourceMappingURL=n2words_de-DE.js.map